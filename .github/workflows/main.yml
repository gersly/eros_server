name: Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: 0e75fd74-d1f5-4de9-874f-ce5d4e823f43
          heroku_app_name: "eros-server" #Must be unique in Heroku
          heroku_email: "gersonlynch@gmail.com"
